1. under lecturer - resources - clicking open a resource tries to open an external link (item url), resulting in 404


1. links for vidoes
since note and file is alomost the same
let's pick file

File, Vidoe, link

works the same, if the link is clicked then let is count

and admin can decide if a course allows vidoes or not
if no use only file and link
this can be set per course and not globally




1. some and most of the texts are too small to read

2. i need all pages in light mode, maybe 1 or 2 components in each or none can have the dark blue background

3. client said that texts are difficult to read



same with everything in admin pages

the button next to destinations in 404 is invisible due to the colors ot the button

site is mainly light more and legible texts

bulid and return the whole project





NB: under the public folder is supposed to be ceo.jpg but i removed it because the file is >22mb. work with that in mind, ill add it later